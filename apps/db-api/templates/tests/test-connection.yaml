apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-api.fullname" . }}-test-connection"
  labels:
    {{- include "db-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
